import { useParams } from "react-router-dom";

const HookPage = () => {
  const {id} = useParams()

  return <div>
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
    HookPage
  </div>;
};
export default HookPage;
